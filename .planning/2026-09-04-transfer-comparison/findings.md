# Findings

- `ageroot_template` is a nested Git repository on `main` and was clean before transfer.
- Outer repository accidentally contains the implementation in `ageroot_core/` plus instantiated setup/docs changes.
